package org.example.class3feb242024;

public class Ifconditionsifelse {
    public static void main(String[] args) {
        double bankBalance=1000.0;
        if(bankBalance<5000) {   //if is true you will get code if false you will get nothing
            System.out.println("lets buy comp");   // if (variable<..) open [ ........} block of codes inside the curly brackets will be executed.
        }


        boolean isGood=false;   // if true will execute it if false will not execute it inside the curly brackets
        if(isGood){
            System.out.println("i will dring it all");    //whatever in between the {} will be executed together.
        }

        // any s.out outside the curly brackets will always execute
        // we can use boolean or relation operation to get true or false results.

        int age=60;
        if(age<=50) {
            System.out.println("you can run"); // we can execute it because is true.
        } else {
            System.out.println("you cannot"); // with false you will execute the else condition.
        }

         int size=500;
        if(size<=700) {
            System.out.println("will start the project");
        }
        }

        }




